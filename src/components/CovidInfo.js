import React from 'react'

const styles = {
    height: '100vh',
    width: '20%',
    padding: '10px',
    textAlign: 'center',
    fontSize: '30px',
};

const CovidInfo = () => {
    return (
    <div style={styles} >
        HEADLINE
    </div>
    )
}

export default CovidInfo